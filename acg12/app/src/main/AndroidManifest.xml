<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.acg12">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="false"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:name=".ACGApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialSheetFab">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.PalettePreviewActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout" />
        <activity
            android:name=".ui.activity.PreviewAlbumActivity"
            android:theme="@style/Theme.MaterialSheetFab.DragPhoto" />
        <activity
            android:name=".ui.activity.PreviewBangumiActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout" />

        <activity android:name=".ui.activity.SkinActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.SettingActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.RecordActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.DownloadActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.CollectActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.SearchActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
        <activity android:name=".ui.activity.AboutActivity"
            android:theme="@style/Theme.MaterialSheetFab.SwipeBackLayout"/>
    </application>

</manifest>